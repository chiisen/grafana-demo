# 📌 Grafana Dashboard 註解快速參考

## 🎯 為什麼使用 description 欄位？

因為 **JSON 不支援註解**（`//` 或 `/* */`），所以我們使用 Grafana 原生的 `description` 欄位。

---

## 📍 在哪裡添加 description？

### 1️⃣ Dashboard 層級（第 2 行）

```json
{
    "description": "Dashboard 的整體說明...",
    "title": "Dashboard Title",
    ...
}
```

### 2️⃣ Panel 層級（每個 Panel 的開頭）

```json
{
    "panels": [
        {
            "description": "這個 Panel 的功能說明...",
            "title": "Panel Title",
            ...
        }
    ]
}
```

### 3️⃣ Variable 層級（每個變數的開頭）

```json
{
    "templating": {
        "list": [
            {
                "description": "這個變數的用途說明...",
                "name": "variable_name",
                ...
            }
        ]
    }
}
```

---

## 🖥️ 在 Grafana UI 中查看

| 位置 | 查看方式 |
|------|---------|
| **Dashboard** | 點擊標題旁的 ℹ️ 圖示<br>或進入 Settings → General |
| **Panel** | 點擊 Panel 標題旁的 ℹ️ 圖示<br>或進入 Edit → Panel options |
| **Variable** | 進入 Settings → Variables<br>點擊變數進行編輯 |

---

## ✍️ Description 應該包含什麼？

✅ **功能說明**：這個元件做什麼？  
✅ **資料來源**：從哪裡取得資料？  
✅ **查詢語法**：使用什麼查詢？（特別是複雜查詢）  
✅ **互動方式**：使用者可以如何操作？  
✅ **注意事項**：有什麼需要特別注意的？  

---

## 📝 範例

### 好的 Description ✅

```json
"description": "顯示每分鐘的日誌數量統計，按 Job 和 Level 分組。使用堆疊長條圖呈現，圖例顯示總計數。支援點擊圖例進行 Drilldown 鑽取，自動過濾下方的日誌面板。查詢語法：sum by (job, level) (count_over_time({job=~\"${job:regex}\"}[1m]))"
```

### 不好的 Description ❌

```json
"description": "日誌圖表"
```

---

## 🔧 驗證 JSON 格式

```bash
python3 -m json.tool dashboard.json > /dev/null && echo "✅ 格式正確" || echo "❌ 格式錯誤"
```

---

## 📚 完整文檔

- **README.md** - 所有 Dashboard 的詳細說明
- **ANNOTATIONS_GUIDE.md** - 註解撰寫完整指南
- **COMPLETION_REPORT.md** - 工作完成報告

---

## ⚡ 快速檢查清單

在提交前確認：

- [ ] Dashboard 有 `description`
- [ ] 每個 Panel 有 `description`
- [ ] 每個 Variable 有 `description`
- [ ] Description 內容清晰完整
- [ ] JSON 格式驗證通過
- [ ] 使用 UTF-8 編碼

---

## 💡 提示

- Description 支援 Unicode（可用繁體中文）
- 可使用 `\n` 換行
- 保持簡潔但完整
- 包含查詢語法有助於理解
