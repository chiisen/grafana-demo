services:
  loki:
    image: grafana/loki:3.5.5
    container_name: loki
    ports:
      - "3100:3100" # 在更嚴格的設置中，考慮限制為內部網絡
    volumes:
      - ./loki-config.yml:/etc/loki/local-config.yaml:ro
      - loki-data:/loki
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - grafana-net

  promtail:
    image: grafana/promtail:3.5
    container_name: promtail
    volumes:
      - ./promtail-config.yml:/etc/promtail/config.yaml:ro
      - promtail-data:/data
      - ../app/code/storage/logs:/var/log:ro # 將主機日誌掛載為唯讀
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command: -config.file=/etc/promtail/config.yaml -config.expand-env=true
    networks:
      - grafana-net

  prometheus:
    image: prom/prometheus:v3.5.0
    container_name: prometheus
    restart: always
    volumes:
      - ./prometheus:/etc/prometheus/:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    ports:
      - 9090:9090
    networks:
      - grafana-net

  node_exporter:
    image: quay.io/prometheus/node-exporter:v1.9.1
    container_name: node_exporter
    restart: unless-stopped
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--path.rootfs=/rootfs'
      - "--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)"
    ports:
      - "9100:9100"
    healthcheck:
      test: [ "CMD", "/bin/true" ]
      interval: 1m
      timeout: 10s
      retries: 3
    networks:
      - grafana-net

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.49.1
    restart: unless-stopped
    container_name: cadvisor
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /etc/machine-id:/etc/machine-id:ro
      - /var/lib/dbus/machine-id:/var/lib/dbus/machine-id:ro
    # cAdvisor 在許多系統上需要提升的權限；儘可能保持最低權限
    privileged: true
    ports:
      - 8080:8080
    networks:
      - grafana-net

  # TODO: 添加 MySQL 導出器 (MySQL exporter)
  # mysql_exporter:
  #   image: prom/mysqld-exporter:v0.17.2
  #   container_name: mysql_exporter
  #   restart: unless-stopped
  #   # 在生產環境中，使用 .env 或 secrets 存儲資料庫憑證
  #   # env_file:
  #   #   - ./mysql_exporter.env
  #   ports:
  #     - "9104:9104"
  #   networks:
  #     - grafana-net

  grafana:
    image: grafana/grafana:12.1
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/provisioning/dashboards:/var/lib/grafana/dashboards:ro
    # environment:
    #   GF_SECURITY_ADMIN_PASSWORD_FILE: /run/secrets/grafana_admin_password
    # secrets:
    #   - grafana_admin_password
    # healthcheck:
    #   test: ["CMD", "wget", "--spider", "http://localhost:3000/api/health"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 5
    networks:
      - grafana-net

  setup:
    image: alpine:latest
    container_name: grafana-setup
    depends_on:
      grafana:
        condition: service_started
    networks:
      - grafana-net
    volumes:
      - ./script:/script:ro
      - grafana-data:/var/lib/grafana
    environment:
      - GRAFANA_HOST=grafana:3000
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
      # GRAFANA_TOKEN is loaded from .env
      - GRAFANA_TOKEN=${GRAFANA_TOKEN}
    entrypoint: [ "/bin/sh", "-c" ]
    command: >
      "apk add --no-cache bash curl jq sqlite && bash /script/init_grafana.sh"
networks:
  grafana-net:
    name: grafana-net

volumes:
  grafana-data:
  loki-data:
  promtail-data:
  prometheus_data:

    # secrets:
    #   grafana_admin_password:
    #     file: ./secrets/grafana_admin_password.txt
